# ─── Set this to your Railway Node backend URL after deploying ───────────────
# Example: VITE_API_BASE=https://raksha-api.up.railway.app
VITE_API_BASE=https://YOUR_RAILWAY_NODE_URL_HERE

# ─── Safe Route Python service URL (proxied through Node, no change needed) ──
# The Node backend reads SAFE_ROUTE_URL from its own env vars on Railway
